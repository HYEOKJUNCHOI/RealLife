# RealLife UI 반복 실수 체크리스트

목적: 매번 스크린샷부터 찍지 않고, 빠른 자체 반복검사로 과거 실수를 먼저 잡는다. 스크린샷은 중요한 흐름 변경 후/사인 전/위험 발견 시 찍는다.

## 반복검사 루프

기본 루프: **실수 → 반성 → 개선 → 재시도**

1. 이번에 고친 화면/흐름을 하나만 정한다.
2. 아래 실수 유형과 코드/레이아웃을 빠르게 대조한다.
3. 실수가 있으면 무엇이 실패했는지 구체적으로 적는다.
4. 왜 실패했는지 반성한다: 성급한 진행, 검증 부족, 잘못된 가정, 하네스 부재, 레이아웃 사각지대 등.
5. 같은 종류를 다시 잡을 수 있게 체크리스트/규칙/테스트/하네스를 개선한다.
6. 가장 작은 안전 단위로 재시도한다.
7. 의미 있는 변경 단위가 끝나면 빌드 → 필요 시 캡처 → 사용자 확인.

## 실수 유형

- 프레임/슬롯 밖으로 삐져나감.
- `overflow: hidden`으로 문제를 숨김.
- 배지/아바타/외곽선/글로우가 서로 겹침.
- 투명 이미지 뒤로 복잡한 UI가 비침.
- 캡슐/버튼/카드 경계가 약해서 구분이 안 됨.
- 글자가 공간 부족으로 어색하게 강제 줄바꿈됨.
- 카드 비율이 300:400 계열에서 벗어나 길쭉하거나 밑이 잘림.
- 주사위/버튼/탭 여백 균형이 깨짐.
- 안내/알림이 떠 있을 때 뒤 UI가 같이 반응함.
- 사회자 캡슐/메인 알림창이 결과마다 다른 디자인으로 튐.
- 카드 공개/정산/도착 알림이 중복으로 뜸.
- 사인 완료 항목을 다시 건드림.

## 발견 기록

- 2026-05-12: 사회자 캡슐 통일 패치 중, 한 번에 여러 항목을 묶어 진행하려는 실수 발생. 이후 하나씩 진행하고 보고하기로 정정.
- 2026-05-12: 스크린샷 분석툴 실패 시 검증한 척하지 말고 사용자에게 캡처를 보여주기로 정정.
- 2026-05-12: 카드 공개 테스트 중 `턴 시작 안내`와 `CARD REVEAL`이 동시에 떠 있는 문제 발견. 사회자 캡슐 모양 통일만 볼 게 아니라, 오버레이 우선순위/동시 표시 금지를 반드시 검사해야 함.
- 2026-05-12: 카드 공개 후 사회자 문구 `카드를 확인했습니다. 화면을 / 터치하면 진행합니다.`처럼 어색하게 자동 줄바꿈됨. 긴 문장을 캡슐에 넣을 때는 자연스러운 의미 단위로 직접 줄바꿈하거나 문구를 줄여야 함. 강제 줄바꿈 방치 금지.
- 2026-05-12: 사회자 캡슐류는 문자열을 그대로 때려 넣지 말고 `HostCapsule`에서 문장부호/의미 단위로 자동 줄바꿈 처리한다. 동적 문구도 캡슐 폭에 맡겨 깨지게 두지 않는다.
