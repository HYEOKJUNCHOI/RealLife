# RealLife UI 5개 테스트 결과 - 2026-05-12

## 테스트 환경
- URL 확인: `http://127.0.0.1:5174/`
- 문서 제목 확인: `RealLife`
- 앱 텍스트 확인 후 캡처 진행.
- 빌드: `npm run build` 성공.
  - JS: `index-BzAiQQg9.js`
  - CSS: `index-CF2nlXn9.css`

## 1. 사회자 캡슐 공통화
- 테스트: 코드 정적 확인 + 실제 첫 권리증 인트로/카드 공개 화면 확인.
- 결과: 부분 통과.
- 확인됨:
  - `HostCapsule` 공통 컴포넌트 존재.
  - 카드 공개/로또/턴 시작/글로벌 알림/첫 권리증 안내가 `HostCapsule` 계열 사용.
- 문제:
  - 카드 공개 중 턴 시작 사회자 캡슐도 같이 남아 있음. 즉 공통화 자체는 됐지만, 표시 우선순위 정리는 실패.

## 2. 카드 공개 화면 정리
- 테스트: 찬스칸 도착을 직접 입력 7로 유도해 `CARD REVEAL` 표시 확인.
- 결과: 부분 통과.
- 확인됨:
  - `CARD REVEAL` 단일 카드 공개 화면 표시.
  - 카드 비율은 `180x240`, 300:400 비율.
  - `tap to reveal` 같은 영어 QA 문구 제거.
  - 카드 뒤집기 후 팡 효과 `💥` 표시.
- 문제:
  - 카드 공개 화면 뒤에 턴 시작 안내가 동시에 남아 있음.

## 3. 글로벌 알림 배경/터치 차단 구조
- 테스트: 코드 정적 확인.
- 결과: 코드상 통과, 실제 흐름 추가 확인 필요.
- 확인됨:
  - 글로벌 알림 레이어에 `bg-black/34` + `backdrop-blur-[2px]` 있음.
  - `onPointerDown={absorbPointer}`로 뒤 UI 클릭 차단 구조 있음.
- 추가 확인 필요:
  - 실제 도착/매입/통행료 알림에서 뒤 UI가 안 눌리는지 직접 확인 필요.

## 4. 첫 권리증 분배 인트로 터치 영역
- 테스트: 새 게임 시작 후 `시작하기` → 권리증 안내 인트로 캡처.
- 결과: 통과 후보.
- 확인됨:
  - 화면 전체 클릭으로 다음 진행 가능.
  - 사회자 캡슐이 공통 스타일로 표시됨.
  - 안내 영역이 넓음.
- 주의:
  - 사인 완료 항목이라 기능을 더 건드리면 안 됨. 현재는 스타일 통일만 확인.

## 5. QA/테스트 문구 제거
- 테스트: 소스 검색.
- 결과: 통과.
- 확인됨:
  - `tap to reveal`, `tap to close`, `터치하면 닫`, `메인 알림창`, `사회자 멘트창`, `>TEST<` 검색 통과.
  - DEBUG 패널의 `TEST`는 `LOG`로 변경됨.

## 발견한 핵심 실수

### 실수
- 카드 공개 오버레이와 턴 시작 오버레이가 동시에 떠 있다.
- 테스트 중 실제 DOM에서 둘 다 확인됨:
  - `자, 다음 플레이어 준비됐습니다!`
  - `CARD REVEAL`

### 반성
- 사회자 캡슐 모양만 통일하고, 오버레이 우선순위/동시 표시 금지까지 같이 확인하지 않았다.
- `화면 하나에 최상위 안내는 하나`라는 사용자의 핵심 요구를 정적 코드 확인만으로 충분히 검증했다고 착각할 뻔했다.

### 개선
- 카드 공개/글로벌 알림/턴 시작 안내는 동시에 떠 있으면 실패로 체크리스트에 추가.
- 다음 수정은 디자인 변경이 아니라 표시 우선순위 정리부터 해야 한다.

### 재시도 대상
- 카드 공개 중에는 턴 시작 안내를 숨기거나 띄우지 않는다.
- 카드 정산/확인 완료 후 필요한 다음 안내만 순서대로 보여준다.

## 캡처 파일
- `C:\Users\gurwn\.openclaw\workspace\test5-1-initial-start.png`
- `C:\Users\gurwn\.openclaw\workspace\test5-2-initial-intro.png`
- `C:\Users\gurwn\.openclaw\workspace\test5-3-after-deal.png`
- `C:\Users\gurwn\.openclaw\workspace\test5-4-card-reveal-overlap.png`
- `C:\Users\gurwn\.openclaw\workspace\test5-5-card-flipped.png`
