# CLAUDE.md

Claude Code가 이 프로젝트에서 작업할 때 지켜야 할 컨벤션 문서.

---

## 프로젝트 개요

**monopoly-npc** — 보드게임 모노폴리에서 은행원·진행자 역할을 NPC가 대신해주는 웹앱.

**한 줄 컨셉**: 물리 보드 + 말 + 주사위만 빌리고, 나머지(돈, 카드, 권리증, 은행원)는 패드가 다 한다.

자세한 내용은 `PROJECT_SPEC.md` 참고. 핵심 룰은 `RULES.md` 참고. 의사결정 히스토리는 `HANDOFF.md` 참고.

---

## 기술 스택

- **프론트엔드**: React 18 + Vite
- **상태 관리**: zustand (단순함, 보일러플레이트 적음) — 미정 시 zustand 우선
- **애니메이션**: framer-motion
- **스타일**: Tailwind CSS
- **배포**: Vercel
- **저장소**: localStorage (게임 진행 상태 저장)
- **백엔드 없음**: 단일 기기 웹앱, Firebase 등 미사용

---

## 환경 제약

- **가로모드 고정** (PWA manifest + CSS orientation 감지)
- **단일 기기** (다중 디바이스 동기화 X)
- **타겟 기기 (반응형 동시 지원, 둘 다 1순위)**:
  - iPad (1133~1366 × 744~1024, CSS 기준)
  - iPhone 17 (874 × 402, CSS 기준)
- **오프라인 작동 가능**

### 반응형 브레이크포인트 (Tailwind 기준)

```
- 기본 (sm 이하): 폭 < 768px → iPhone 17 가로 (874×402)
- md: 폭 ≥ 768px → iPad mini, iPad Air
- lg: 폭 ≥ 1180px → iPad Pro
```

### 화면별 레이아웃 차이 (필수)

| 영역 | iPhone (좁고 낮음) | iPad (넓고 높음) |
|---|---|---|
| 셋업 화면 | 세로 스크롤, 단계별 표시 | 2단 레이아웃 (왼쪽 정보, 오른쪽 옵션) |
| 메인 게임 | 탭 전환 (보유/액션/NPC) | 3단 동시 표시 |
| 부동산 카드 모달 | 풀스크린 | 중앙 작은 모달 |
| NPC 멘트 | 하단 고정 토스트 | 상단 고정 영역 |

### 반응형 작업 시 주의

- iPhone 17 가로는 높이가 매우 좁음 (402px) → 가로 영역 활용 우선
- 부동산 카드 22개 + 자산 정보 다 넣을 수 없음 → 탭/모달 분리 필수
- 터치 타겟 최소 44×44 (Apple HIG)
- safe area 처리 필수: `env(safe-area-inset-*)`
- 가로모드 회전 안 한 사용자 → "기기를 가로로" 안내 화면

---

## 커뮤니케이션 스타일

### 언어
- **한국어로 응답** (영어 답변 금지)
- 존댓말 사용
- 직설적이고 솔직한 톤. 감싸거나 과도한 친절 X. 구체적 피드백 우선.

### 영어 키워드 표기
- 처음 나오는 영어 키워드에 한국어 발음/뜻 병기
  - 예: `define(디파인, 정의하다)`
  - 예: `mortgage(모기지, 저당)`
- 같은 대화에서 한 번 나온 단어는 반복 표기 안 함

### 코드 설명 형식
1. 코드 블록 먼저 띄움
2. 구분선(`---`) 후 영어 단어 본뜻을 `*` bullet로 표기
3. 예시:
   ````
   ```js
   const greet = (name) => `Hello, ${name}`;
   ```
   
   ---
   
   * greet(그렛: 인사하다)
   * const(콘스트: 상수)
   ````

---

## 작업 원칙

### 1. 스코프 크립 방지 (가장 중요)
- **MVP 범위 외 작업 금지**
- PROJECT_SPEC.md의 "MVP 범위" 섹션이 절대 기준
- V2 기능을 살짝 끼워넣으려 하지 않음
- 사용자가 명시적으로 V2 작업 요청한 경우만 예외

### 2. PROJECT_SPEC.md가 진실의 원천
- 새 기능 추가/룰 변경 전 반드시 SPEC 확인
- 코드와 SPEC이 충돌하면 SPEC이 우선
- SPEC 변경 시 사용자에게 먼저 확인

### 3. 데이터/룰 분리 원칙
- 룰은 `src/engine/`에 집중
- 데이터는 `src/boards/`, `src/data/`에 JSON으로
- 룰 엔진은 보드 데이터를 모름 (보드 ID로만 동작)

### 4. 제안하기 전 SPEC 읽기
- 사용자가 모호하게 요청하면, SPEC에서 답을 찾을 수 있는지 먼저 확인
- 없으면 SPEC에 정의된 컨벤션을 추론해서 제안

---

## 폴더 구조

```
monopoly-npc/
├── PROJECT_SPEC.md          # 전체 명세
├── RULES.md                 # 핵심 룰 압축본
├── HANDOFF.md               # 의사결정 히스토리
├── CLAUDE.md                # 이 파일
├── package.json
├── vite.config.js
├── tailwind.config.js
├── public/
│   └── assets/              # NPC 일러스트, 말 아이콘
├── src/
│   ├── boards/              # 보드별 데이터
│   │   ├── korea.json       # 한국판 (MVP)
│   │   └── japan.json       # 일본판 (V2)
│   ├── data/                # 글로벌 데이터
│   │   ├── tokens.json      # 표준 말 8종 (참고용, MVP에서는 캐릭터 사용)
│   │   ├── characters.json  # 플레이어 캐릭터 (한국판 4종 MVP)
│   │   ├── events.json      # 이벤트 카드
│   │   └── help-texts.json  # 셋업 화면 옵션 도움말 텍스트
│   ├── engine/              # 룰 엔진
│   │   ├── rules.js         # 게임 룰 로직
│   │   ├── gameState.js     # 상태 관리
│   │   ├── inflation.js     # 인플레 계산
│   │   ├── loan.js          # 대출/신용대출
│   │   └── trade.js         # 거래 시스템
│   ├── components/          # UI 컴포넌트
│   │   ├── TileCard.jsx
│   │   ├── DiceRoller.jsx
│   │   ├── PlayerStatus.jsx
│   │   ├── NpcDialog.jsx
│   │   └── ...
│   ├── screens/             # 화면 단위
│   │   ├── GameSetup.jsx
│   │   ├── GameMain.jsx
│   │   └── ...
│   ├── stores/              # zustand 스토어
│   │   └── gameStore.js
│   ├── App.jsx
│   └── main.jsx
└── index.html
```

---

## 코드 컨벤션

### 파일명
- 컴포넌트: PascalCase (`TileCard.jsx`)
- 일반 모듈: camelCase (`gameState.js`)
- JSON 데이터: kebab-case 또는 단순명 (`korea.json`, `events.json`)

### import 순서
1. React / 외부 라이브러리
2. 내부 모듈 (engine, stores)
3. 컴포넌트
4. 데이터/JSON
5. 스타일

### 함수 스타일
- 화살표 함수 우선
- 순수 함수 우선 (사이드 이펙트 분리)
- 룰 엔진은 모두 순수 함수로 (테스트 가능)

### 주석
- 한국어 주석 OK
- 룰 엔진의 비직관적 로직에 반드시 주석
- 예: `// 4% 복리, 만원 단위 반올림`

---

## 데이터 파일 사용

### 보드 데이터 로딩
```js
import koreaBoard from '@/boards/korea.json';
// 또는
const board = await import(`@/boards/${boardId}.json`);
```

### JSON 수정 금지
- `src/boards/*.json`은 검증 완료된 데이터
- 수정해야 할 일 생기면 사용자에게 먼저 확인

---

## 일러스트 제작 (캐릭터 / NPC)

### 원칙
- **MVP에서는 일러스트 없이 이모지로 대체 가능** (게임 로직 검증 우선)
- 일러스트 필요한 시점에 **Claude Design** (`https://claude.ai/design`) 에서 생성

### 한국판 캐릭터 4종 (MVP)
| ID | 이름 | 임시 이모지 | 일러스트 프롬프트 힌트 |
|---|---|---|---|
| `scholar` | 선비 | 🎓 | 조선 시대 갓 쓴 선비, 두루마기, 책 든 모습 |
| `merchant` | 객주 | 💰 | 조선 시대 객주, 도포 입고 주판 든 모습 |
| `gisaeng` | 기생 | 🎭 | 조선 시대 기생, 한복, 부채 든 모습 |
| `farmer` | 농부 | 🌾 | 조선 시대 농부, 패랭이 모자, 호미 든 모습 |

### 일러스트 스타일 가이드
- 모노폴리 보드게임 분위기 (밝고 친근한 톤)
- 4명 캐릭터 스타일 통일 (같은 작가가 그린 듯한 일관성)
- 각 캐릭터 2종 필요: **등 돌린 실루엣 + 정면 모습**
- 가로모드 화면용 (사이즈 200×300 정도)

### NPC (한국판 호스트)
- ID: `korea_banker`
- 이름: 조선 객주
- 캐릭터: 정중하고 격식 있는 옛스러운 한국어 사용

---

## 단계별 우선순위

### 현재 단계: 룰 엔진 + 화면 와이어프레임

### 다음 작업 후보 (순서 추천)
1. NPC voiceLines 작성 (한국 객주 컨셉, 30~50개)
2. 룰 엔진 코드 (인플레, 대출, 거래)
3. 화면 와이어프레임
4. 게임 셋업 화면
5. 부동산 카드 컴포넌트
6. 턴 흐름 상태 머신

작업 시작 전 사용자에게 어느 것부터 할지 확인.

---

## 주의 사항

### 절대 하지 않을 것
- ❌ Firebase, 백엔드 추가
- ❌ V2 기능 미리 구현 (이벤트 발동, 일본판 UI 등)
- ❌ 표준 모노폴리 룰 마음대로 변경 (SPEC에 명시된 커스텀 룰만)
- ❌ 모바일 세로모드 지원

### 항상 확인할 것
- ✅ MVP 범위에 들어가는 작업인가?
- ✅ SPEC과 일치하는가?
- ✅ 데이터/룰 분리 원칙을 지키는가?
- ✅ 가로모드 가정인가?
