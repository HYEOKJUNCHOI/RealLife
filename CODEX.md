# CODEX.md

Codex가 RealLife 프로젝트에서 작업할 때 지킬 규칙.

---

## 기본 원칙

- 사용자는 한국어로 소통한다. Codex도 한국어로 답한다.
- 사용자가 수정 요청을 하면 먼저 적용하고, 완료 후 짧게 요약한다.
- 기존 구조와 스타일을 우선 따른다.
- MVP 범위 밖 기능은 임의로 크게 확장하지 않는다.
- 사용자 변경사항을 되돌리지 않는다.

---

## AI 코딩 행동 원칙

- 코딩 전에 요청의 의도와 영향 범위를 먼저 생각한다.
- 애매한 요구사항은 임의로 해석하지 말고, 필요한 경우 짧게 확인한다.
- 요청 범위를 벗어난 기능 추가나 리팩토링을 하지 않는다.
- 변경한 모든 줄은 사용자의 요청과 직접 연결되어야 한다.
- 기존 코드 스타일과 구조를 우선 따른다.
- 단순한 해결책을 먼저 선택한다.
- 작업 후 수정 내용과 검증 결과를 짧게 보고한다.

---

## 작업 전 확인

작업을 시작할 때 우선 확인할 문서:

- `PROJECT_SPEC.md` — 전체 명세
- `RULES.md` 또는 `RULEBOOK.md` — 게임 규칙
- `HANDOFF.md` — 이전 작업 맥락
- `TASKS.md` / `TODO.md` — 남은 작업
- `CODEX.md` — Codex 작업 규칙

---

## 커밋 규칙

Codex 커밋 메시지는 반드시 `[CD]`로 시작한다.

```text
[CD] 타입: 한글 설명
```

타입:

- `feat` — 새 기능
- `fix` — 버그 수정
- `style` — 화면, 색상, 배치 등 스타일 변경
- `refactor` — 구조 개선
- `docs` — 문서 수정
- `chore` — 설정, 빌드, 기타 작업

예시:

```text
[CD] style: 선택 전 캐릭터 이름 테두리 금색 적용
```

---

## 작업 기록 요약

작업을 정리할 때는 한글로 짧게 남긴다.

```text
[CD] style: 선택 전 캐릭터 이름 테두리 금색 적용

- 미선택 캐릭터 카드의 이름 뱃지 테두리를 금색으로 변경
- 선택된 캐릭터는 기존 플레이어 컬러 표시 유지
```

---

## 핸드오프 규칙

사용자가 "핸드오프 해"라고 말하면 다음 순서로 정리한다.

1. 현재 작업을 안전한 상태까지 마무리한다.
2. 작업 내용과 수정 파일을 한글로 요약한다.
3. `HANDOFF.md`에 다음 세션이 알아야 할 내용을 추가한다.
4. 가능하면 `git add` -> `commit` -> `push`를 수행한다.

---

## 현재 Codex 식별자

- 이름: Codex
- 커밋 접두어: `[CD]`
