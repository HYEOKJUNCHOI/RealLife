# CODEX.md

Codex가 RealLife 프로젝트에서 작업할 때 지킬 규칙.

---

## 기본 원칙

- 사용자는 한국어로 소통한다. Codex도 한국어로 답한다.
- 사용자가 수정 요청을 하면 먼저 적용하고, 완료 후 짧게 요약한다.
- 기존 구조와 스타일을 우선 따른다.
- MVP 범위 밖 기능은 임의로 크게 확장하지 않는다.
- 사용자 변경사항을 되돌리지 않는다.

---

## AI 코딩 행동 원칙

- 코딩 전에 요청의 의도와 영향 범위를 먼저 생각한다.
- 애매한 요구사항은 임의로 해석하지 말고, 필요한 경우 짧게 확인한다.
- 요청 범위를 벗어난 기능 추가나 리팩토링을 하지 않는다.
- 변경한 모든 줄은 사용자의 요청과 직접 연결되어야 한다.
- 기존 코드 스타일과 구조를 우선 따른다.
- 단순한 해결책을 먼저 선택한다.
- 작업 후 수정 내용과 검증 결과를 짧게 보고한다.

---

## 글로벌 UI 레이아웃 원칙 / 모든 프로젝트 공통

이 규칙은 RealLife뿐 아니라 어떤 프로젝트를 하든 적용한다.

- 틀/슬롯/패널/버튼/카드영역/컨테이너를 만들어뒀으면 내부 요소는 반드시 그 정해진 공간 안에 맞춘다.
- 요소가 크면 넘치게 두거나 겹치게 하지 말고, 요소 자체를 줄이거나 비율/배치를 조정해서 슬롯 안에 들어가게 한다.
- 카드슬롯, 주사위판, 알림창, 로딩틀, 버튼 등 모든 UI에 적용한다.
- `overflow: hidden`으로 잘리게 두는 것은 해결이 아니다. 잘리지 않게 맞추는 것이 원칙이다.
- 공간 부족 때문에 어색한 강제 줄바꿈이 생기는 것도 레이아웃 실패로 본다. 문구/폭/폰트/배치를 조정한다.
- 삐져나감, 겹침, 강제 절단, 가려짐, 비침, 경계 불명확은 미완성 상태로 본다.
- “작동은 한다”는 완료 기준이 아니다. 배지, 아바타, 주사위, 카드, 캡슐, 텍스트가 틀 안에 억지로 끼워진 느낌이면 아직 실패다.
- 투명 이미지를 복잡한 UI 위에 그대로 올려 뒤가 비치게 만들지 않는다. 얼굴/초상화는 의도적 마스크와 불투명 배경을 둔다.
- 캡슐/버튼/카드의 경계와 대비는 한눈에 읽혀야 한다. 흐릿한 유리톤끼리 겹쳐 경계가 안 보이는 디자인은 금지한다.
- UI 완료 전에는 전체 구도를 보고 여백, 정렬, 경계, 가독성, 컨테이너 내부 맞춤을 반드시 확인한다.

---

## 작업 전 확인

작업을 시작할 때 우선 확인할 문서:

- `PROJECT_SPEC.md` — 전체 명세
- `RULES.md` 또는 `RULEBOOK.md` — 게임 규칙
- `HANDOFF.md` — 이전 작업 맥락
- `TASKS.md` / `TODO.md` — 남은 작업
- `CODEX.md` — Codex 작업 규칙

---

## 커밋 규칙

Codex 커밋 메시지는 반드시 `[CD]`로 시작한다.

```text
[CD] 타입: 한글 설명
```

타입:

- `feat` — 새 기능
- `fix` — 버그 수정
- `style` — 화면, 색상, 배치 등 스타일 변경
- `refactor` — 구조 개선
- `docs` — 문서 수정
- `chore` — 설정, 빌드, 기타 작업

예시:

```text
[CD] style: 선택 전 캐릭터 이름 테두리 금색 적용
```

---

## 작업 기록 요약

작업을 정리할 때는 한글로 짧게 남긴다.

```text
[CD] style: 선택 전 캐릭터 이름 테두리 금색 적용

- 미선택 캐릭터 카드의 이름 뱃지 테두리를 금색으로 변경
- 선택된 캐릭터는 기존 플레이어 컬러 표시 유지
```

---

## 핸드오프 규칙

사용자가 "핸드오프 해"라고 말하면 다음 순서로 정리한다.

1. 현재 작업을 안전한 상태까지 마무리한다.
2. 작업 내용과 수정 파일을 한글로 요약한다.
3. `HANDOFF.md`에 다음 세션이 알아야 할 내용을 추가한다.
4. 가능하면 `git add` -> `commit` -> `push`를 수행한다.

---

## 현재 Codex 식별자

- 이름: Codex
- 커밋 접두어: `[CD]`

---

## 캐릭터 생성 UX 원칙

RealLife의 캐릭터 추가 기능은 다음 흐름을 기본값으로 한다.

1. 관리자 페이지에서 캐릭터 이름을 먼저 입력한다.
2. 입력한 이름이 자동으로 들어간 일러스트 프롬프트를 복사한다.
3. GPT에서 일러스트를 생성하고 마음에 드는 이미지를 다운로드한다.
4. 관리자 페이지에 이미지를 첨부한다.
5. 앱이 이미지를 게임 카드/로스터에 맞게 자동 보정한다.
6. 만들기 버튼을 누르면 이름과 이미지가 합쳐져 캐릭터가 생성된다.

초기 구현은 localStorage 기반의 로컬 생성 기능으로 간다.

- 컴퓨터 로컬에서 캐릭터를 만들고 테스트한다.
- 마음에 드는 캐릭터는 Codex가 프로젝트 정식 자산으로 승격한다.
- 정식 자산으로 승격한 뒤 git push 및 Vercel 배포를 하면 iPad에서도 기본 캐릭터처럼 보인다.
- DB, Vercel Blob, API 이미지 생성은 나중 단계로 둔다.

Codex는 사용자가 제안한 흐름보다 더 덜 헷갈리고 오래 갈 UX가 보이면 먼저 제안한다.

- 예: "프롬프트 복사 후 이름 수정"보다 "이름 입력 후 프롬프트 자동 생성"이 좋으면 먼저 말한다.
- 단, 지금 필요한 범위를 과하게 키우지 않는다.
- 캐릭터 생성은 나중에 API 이미지 생성으로 확장 가능하게 설계한다.

캐릭터 세계관 기준:

- 조선 인물만이 아니라 세계의 유명한 역사 인물, 위인, 책에 등장할 법한 인물까지 확장한다.
- 인물들은 타임머신/포탈을 타고 현대 서울에 도착해 RealLife 부동산 게임을 플레이한다.
- 포탈은 세계관 장치이며, UI를 가리는 주인공 요소가 되면 안 된다.
