# RealLife UI 5개 변경 검토 보고 - 2026-05-12

## 검토 대상 5개

### 1. 사회자 캡슐 공통화
- 변경: `HostCapsule` 공통 컴포넌트를 만들고 카드 공개/로또/턴 시작/글로벌 알림 사회자 멘트를 같은 초록 캡슐 계열로 맞춤.
- 자체검사:
  - 결과 종류마다 캡슐 색이 튀는 문제 완화.
  - 캡슐 안 아이콘/문구는 같은 구조.
  - 긴 문구는 `keep-all` 유지라 강제 줄바꿈 위험은 낮지만, 아주 긴 멘트는 별도 확인 필요.

### 2. 카드 공개 화면 정리
- 변경: 카드 공개 상단 사회자 멘트는 공통 캡슐로 분리. 카드 본체는 `180x240`으로 조정해 300:400 비율에 맞춤.
- 변경: 뒤집은 뒤에는 버튼 없이 화면 터치로 진행. `tap to reveal` 같은 QA/영어 안내 제거.
- 자체검사:
  - 카드 비율은 0.75로 맞음.
  - 별도 `카드확인 정산공개` 버튼은 제거됨.
  - 팡 효과는 유지.

### 3. 글로벌 알림 배경/터치 차단
- 변경: 글로벌 알림 전체 레이어에 `bg-black/34 + backdrop-blur` 추가. 뒤 보드는 보이되 클릭은 막힘.
- 변경: 사회자 멘트는 공통 캡슐, 메인 알림창은 아래 별도 패널 구조 유지.
- 자체검사:
  - 배경 UI가 그냥 투명하게 비치는 느낌은 줄임.
  - `onPointerDown`에서 prevent/stop 처리 유지.
  - 메인 알림창 경계는 `border-[3px] border-ink-line`로 명확.

### 4. 첫 권리증 분배 인트로 터치 영역
- 변경: 첫 권리증 안내는 화면 전체 버튼으로 받고, 내부 카드 영역을 넓힘.
- 변경: 사회자 멘트도 공통 캡슐 사용.
- 자체검사:
  - 아무 곳 터치 → 진행 구조 유지.
  - 뒤 UI 반응 차단을 위한 전체 레이어 구조 유지.
  - 이 항목은 사인 완료 항목이므로 기능을 크게 바꾸지 않고 스타일 통일만 반영.

### 5. QA/테스트 문구 제거 확인
- 변경: `tap to reveal`, `tap to close`, `터치하면 닫기` 계열 화면 문구 제거/공백화.
- 추가 수정: DEBUG 패널 헤더의 `TEST`를 `LOG`로 변경.
- 자체검사:
  - `Select-String`으로 QA 문구 검색 시 실제 표시 문구는 제거됨.
  - 단, DEBUG LOG 버튼/패널은 개발 확인용 기능이라 남아 있음.

## 검증
- `npm run build` 성공.
- 산출물:
  - JS: `index-BzAiQQg9.js`
  - CSS: `index-CF2nlXn9.css`
- 경고:
  - JS chunk 500kB 초과 경고는 기존과 같은 번들 크기 경고. 이번 UI 변경 실패는 아님.

## 다음 확인 필요
- 사용자가 직접 화면으로 볼 항목:
  1. 사회자 캡슐이 모든 결과에서 같은 느낌인지.
  2. 카드 공개 화면이 너무 작거나 크지 않은지.
  3. 글로벌 알림 뒤 배경 블러/어둡기가 적당한지.
  4. 첫 권리증 안내가 사인했던 느낌을 해치지 않았는지.
  5. 남은 QA 문구가 실제 화면에 보이지 않는지.
